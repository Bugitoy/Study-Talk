import { PrismaClient } from '@prisma/client';
const prisma = new PrismaClient();

const Laos_universities = [

    // Laos
  
    { name: "National University of Laos", domain: "nuol.edu.la", region: "Asia", country: "Laos", isVerified: true },
]